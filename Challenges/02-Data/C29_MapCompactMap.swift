// ════════════════════════════════════════════════════════════════════════
//  Challenge 29 · map, compactMap, and flatMap                 Difficulty ★☆☆
//  Interview question: "When using arrays, what's the difference between
//  `map()` and `compactMap()`?"
// ════════════════════════════════════════════════════════════════════════
//
//  BACKGROUND
//  • `map` transforms every element – output count == input count.
//  • `compactMap` transforms and throws away any nil results.
//  • `flatMap` transforms each element into a sequence and flattens them.
//
//  YOUR TASK (no `for` loops!)
//  1. `parseScores(_:)` – trim whitespace, convert to Int, drop failures.
//  2. `initials(of:)` – the uppercased first character of each name
//     ("" for an empty name – the output must stay the same length).
//  3. `validWebURLs(_:)` – keep only strings that make a URL whose scheme is
//     "http" or "https" and which has a host.
//  4. `allTags(_:)` – flatten the tags of every post into one array.
// ════════════════════════════════════════════════════════════════════════

import Foundation

func parseScores(_ raw: [String]) -> [Int] {
    // TODO
    []
}

func initials(of names: [String]) -> [String] {
    // TODO
    []
}

func validWebURLs(_ strings: [String]) -> [URL] {
    // TODO
    []
}

func allTags(_ postTags: [[String]]) -> [String] {
    // TODO
    []
}
